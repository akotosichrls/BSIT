#include <stdio.h>

int main()
{   
    // initialization
    int i = 1;

    // i <= 50
    while(i <= 50)
    {
        if(i % 2 != 0)  // i % 2 != 0 and to determine if odd number
        {
            printf("%d\n", i); // display
        }
        i++;
    }
    
    return 0;

}
