#include <stdio.h>

int main() {
    int expenses;
    int sum;
    
    while (expenses != -1){
        printf ("expenses: ");
        scanf ("%d", &expenses);
        sum += expenses;
    }
    printf ("total is %d ",sum + 1);
    return 0;
}
